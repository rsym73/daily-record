# 严格的每日锁定与断链重置机制

应用的核心机制是「必须每天完成记录」：日期 D 的记录页在 D+1 凌晨 1 点（设备本地时间）冻结，冻结时若未「完成」，后续页锁死（断链），唯一出路是「清除连续天数、重新开始」。我们选择了最严格的版本——不允许提前写未来、不允许补写过去、没有宽限期——因为任何放宽都会让「必须每天记录」失去约束力。曾考虑「允许补写但 streak 归零」和「次日凌晨宽限期」，均被否决，以保持规则的纯粹与可预期。
