## Agent skills

### Issue tracker

Issues 和 PRD 作为 markdown 文件存放在本仓库的 `.scratch/<feature>/` 下。详见 `docs/agents/issue-tracker.md`。

### Triage labels

使用默认的标准角色字符串（needs-triage、needs-info、ready-for-agent、ready-for-human、wontfix）；实现完成后标 `done`。详见 `docs/agents/triage-labels.md`。

### Domain docs

单一上下文布局 —— 根目录的 `CONTEXT.md`。详见 `docs/agents/domain.md`。
